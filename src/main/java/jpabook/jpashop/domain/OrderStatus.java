package jpabook.jpashop.domain;

// ORDER 작성한건 public static final OrderStatus ORDER = new OrderStatus(); 와 같은 의미
// 옆에 CANCEL 도 동일

public enum OrderStatus {
     // ORDER, CANCEL
    ORDER, CANCEL
}
